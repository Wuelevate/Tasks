// For Loop
for (let i = 1; i <= 5; i++) {
    console.log(`Iteration ${i}`);
}

// While Loop
let count = 3;
while (count > 0) {
    console.log(`Countdown: ${count}`);
    count--;
}

// Do...While Loop
let num = 0;
do {
    console.log(`Number is: ${num}`);
    num++;
} while (num < 3);
